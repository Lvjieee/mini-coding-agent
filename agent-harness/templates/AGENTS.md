# 工作区规则（模板）

> 复制到目标工作区根目录并按项目填写。此文件作为长期规则进入稳定前缀，
> 保持精炼：详细知识放 docs/ 供按需查询，这里只做目录入口。

## 项目概况

- 用一两句话说明这个仓库是什么、给谁用。
- 目录结构：列出关键目录及职责（防止 Agent 在错误位置写文件）。

## 构建与验证

- 安装依赖：`<命令>`
- 运行测试：`<命令>`
- Lint / 类型检查：`<命令>`

## 约定

- 代码风格、命名、分层规则（能写成 lint 规则的优先接入传感器，而不是只写在这里）。
- 禁止事项：如不得改动 `vendor/`、不得提交生成产物。

## 深入文档

- 架构说明：docs/architecture.md
- 常见问题：docs/faq.md
